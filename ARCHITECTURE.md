# Архитектура проекта Pizza

## 📋 Общая структура

Проект использует **Redux Toolkit** для управления состоянием. Вместо локального состояния (useState) теперь все данные хранятся в централизованном хранилище (store).

---

## 🗂️ Структура папок

```
src/
├── store/              # Redux хранилище
│   ├── index.ts       # Главный store
│   ├── hooks.ts       # Типизированные хуки
│   └── slices/        # Слайсы (части состояния)
│       ├── filterSlice.ts    # Фильтры (категория, сортировка, поиск)
│       ├── cartSlice.ts      # Корзина
│       └── pizzasSlice.ts    # Список пицц
├── components/        # Компоненты
├── pages/            # Страницы
├── types/            # TypeScript типы
├── constants/        # Константы
└── utils/            # Утилиты
```

---

## 🔄 Как работает Redux Toolkit

### 1. **Store (Хранилище)** - `src/store/index.ts`

Это централизованное хранилище всех данных приложения. Как глобальная база данных.

```typescript
store = {
  filter: { categoryId, sortType, searchValue },
  cart: { items, totalPrice, totalCount },
  pizzas: { items, isLoading, error }
}
```

### 2. **Slices (Слайсы)** - части состояния

Каждый слайс управляет своей частью данных:

#### **filterSlice** - Фильтры
- Хранит: категорию, сортировку, поиск
- Действия: `setCategoryId`, `setSortType`, `setSearchValue`

#### **cartSlice** - Корзина
- Хранит: товары в корзине, общую сумму, количество
- Действия: `addItem`, `removeItem`, `incrementItem`, `decrementItem`, `clearCart`

#### **pizzasSlice** - Пиццы
- Хранит: список пицц, состояние загрузки, ошибки
- Асинхронное действие: `fetchPizzas` (загружает пиццы с сервера)

### 3. **Actions (Действия)** - команды для изменения состояния

Вместо прямого изменения состояния, мы отправляем "действия":

```typescript
// ❌ Раньше (локальное состояние):
setCategoryId(1)

// ✅ Теперь (Redux):
dispatch(setCategoryId(1))
```

---

## 🔌 Как компоненты получают данные

### **useAppSelector** - читать данные из store

```typescript
// Читаем данные из store
const { items, totalPrice } = useAppSelector((state) => state.cart)
const categoryId = useAppSelector((state) => state.filter.categoryId)
```

### **useAppDispatch** - отправлять действия

```typescript
// Отправляем действие для изменения состояния
const dispatch = useAppDispatch()
dispatch(addItem({ pizza, type, size }))
```

---

## 📱 Как работает приложение пошагово

### **1. Запуск приложения** (`src/main.tsx`)

```typescript
<Provider store={store}>  // Оборачиваем приложение в Provider
  <BrowserRouter>
    <App />
  </BrowserRouter>
</Provider>
```

Provider делает store доступным для всех компонентов.

### **2. Главная страница** (`src/pages/Home.tsx`)

**Что происходит:**
1. Компонент читает фильтры из store: `categoryId`, `sortType`, `searchValue`
2. Когда фильтры меняются → запускается `useEffect`
3. `useEffect` вызывает `dispatch(fetchPizzas())` → загружает пиццы с сервера
4. Пиццы сохраняются в `state.pizzas.items`
5. Компонент читает пиццы из store и отображает их

**Код:**
```typescript
// Читаем данные
const { items, isLoading } = useAppSelector((state) => state.pizzas)
const { categoryId, sortType } = useAppSelector((state) => state.filter)

// Когда фильтры меняются → загружаем пиццы
useEffect(() => {
  dispatch(fetchPizzas())  // Асинхронная загрузка
}, [categoryId, sortType, searchValue])
```

### **3. Компонент Categories** (`src/components/Categories.tsx`)

**Что делает:**
- Показывает список категорий
- При клике отправляет действие `setCategoryId`

**Код:**
```typescript
// Читаем текущую категорию
const categoryId = useAppSelector((state) => state.filter.categoryId)

// При клике меняем категорию
onClick={() => dispatch(setCategoryId(index))}
```

**Что происходит:**
1. Пользователь кликает на категорию
2. Вызывается `dispatch(setCategoryId(1))`
3. `filterSlice` обновляет `state.filter.categoryId = 1`
4. `Home` видит изменение → запускается `useEffect` → загружаются новые пиццы

### **4. Компонент PizzaBlock** (`src/components/PizzaBlock/index.tsx`)

**Что делает:**
- Показывает одну пиццу
- Позволяет выбрать тип теста и размер
- Добавляет пиццу в корзину

**Код:**
```typescript
// Читаем корзину, чтобы показать количество
const cartItems = useAppSelector((state) => state.cart.items)
const cartItem = cartItems.find((item) => item.id === currentItemId)

// Добавляем в корзину
const onClickAdd = () => {
  dispatch(addItem({ pizza, type: activeType, size: sizes[activeSize] }))
}
```

**Что происходит:**
1. Пользователь выбирает тип и размер
2. Нажимает "Добавить"
3. Вызывается `dispatch(addItem(...))`
4. `cartSlice` добавляет товар в `state.cart.items`
5. Автоматически пересчитывается `totalPrice` и `totalCount`
6. Header видит изменения и обновляет счетчик

### **5. Страница Cart** (`src/pages/Cart.tsx`)

**Что делает:**
- Показывает все товары в корзине
- Позволяет изменять количество, удалять товары

**Структура:**
```
Cart (главный компонент)
├── CartHeader (заголовок + кнопка очистки)
├── CartItem (каждый товар) × N
└── CartBottom (итоговая сумма + кнопки)
```

**Код:**
```typescript
// Читаем корзину
const { items, totalPrice, totalCount } = useAppSelector((state) => state.cart)

// Если корзина пуста → показываем CartEmpty
if (items.length === 0) return <CartEmpty />

// Иначе показываем товары
return (
  <CartHeader />
  {items.map(item => <CartItem item={item} />)}
  <CartBottom totalCount={totalCount} totalPrice={totalPrice} />
)
```

### **6. Компонент CartItem** (`src/components/Cart/CartItem.tsx`)

**Что делает:**
- Показывает один товар в корзине
- Кнопки + / - для изменения количества
- Кнопка удаления

**Код:**
```typescript
// Увеличить количество
onClick={() => dispatch(incrementItem(item.id))}

// Уменьшить количество
onClick={() => dispatch(decrementItem(item.id))}

// Удалить товар
onClick={() => dispatch(removeItem(item.id))}
```

---

## 🔄 Поток данных (Data Flow)

### Пример: Добавление пиццы в корзину

```
1. Пользователь кликает "Добавить" в PizzaBlock
   ↓
2. dispatch(addItem({ pizza, type, size }))
   ↓
3. cartSlice обрабатывает действие
   - Находит существующий товар или создает новый
   - Добавляет в state.cart.items
   - Пересчитывает totalPrice и totalCount
   ↓
4. Все компоненты, которые читают state.cart, автоматически обновляются:
   - PizzaBlock (показывает количество)
   - Header (показывает сумму и количество)
   - Cart (если открыта)
```

### Пример: Изменение категории

```
1. Пользователь кликает на категорию в Categories
   ↓
2. dispatch(setCategoryId(1))
   ↓
3. filterSlice обновляет state.filter.categoryId = 1
   ↓
4. Home видит изменение в useEffect
   ↓
5. dispatch(fetchPizzas())
   ↓
6. pizzasSlice загружает пиццы с сервера с фильтром category=1
   ↓
7. state.pizzas.items обновляется
   ↓
8. Home отображает новые пиццы
```

---

## 🎯 Ключевые отличия от старого кода

### ❌ Раньше (локальное состояние):
```typescript
// В каждом компоненте свое состояние
const [categoryId, setCategoryId] = useState(0)
const [cart, setCart] = useState([])

// Передаем через props
<Home searchValue={searchValue} setSearchValue={setSearchValue} />
```

### ✅ Теперь (Redux):
```typescript
// Все состояние в store
const categoryId = useAppSelector((state) => state.filter.categoryId)
const cart = useAppSelector((state) => state.cart.items)

// Не нужно передавать через props
<Home />  // Компонент сам читает из store
```

---

## 📚 Полезные файлы

### **types/index.ts** - Типы данных
Определяет структуру данных (Pizza, CartItem, и т.д.)

### **constants/index.ts** - Константы
Хранит неизменяемые данные (категории, типы теста, сортировки)

### **utils/formatPrice.ts** - Утилиты
Вспомогательные функции (форматирование цены)

### **store/hooks.ts** - Типизированные хуки
`useAppDispatch` и `useAppSelector` с правильными типами

---

## 🎓 Резюме

1. **Store** - централизованное хранилище всех данных
2. **Slices** - части состояния (filter, cart, pizzas)
3. **Actions** - команды для изменения состояния
4. **useAppSelector** - читать данные
5. **useAppDispatch** - отправлять действия
6. **Компоненты** - читают из store и отправляют действия

Вся логика состояния вынесена из компонентов в store. Компоненты стали проще и чище.

